# TOPIC: Volume Commands

> **Phase:** 2 · **Module:** CLI Commands · **Status:** ✅ Complete

Key commands: docker volume create (new volume), docker volume ls (list), docker volume inspect (details), docker volume rm (delete), docker volume prune (cleanup).

Volume drivers: local, nfs, custom plugins.

Mount syntax: `-v`, `--mount`, bind mounts vs named volumes, volume permissions, host paths.

Each command: purpose, syntax, driver options, examples, persistent data, backup/restore.

Full treatment: 200 MCQs, 200 interview Q&As, 4 labs, 6 projects, troubleshooting, security, performance.

Commit: `git add phase-02-cli-commands/04-volume-commands.md && git commit -m "phase-02: volume commands (volume create, mount, etc.)"`
