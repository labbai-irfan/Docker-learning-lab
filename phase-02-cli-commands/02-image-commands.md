# TOPIC: Image Commands

> **Phase:** 2 · **Module:** CLI Commands · **Status:** ✅ Complete

Key commands: docker build (create image), docker pull (download), docker push (upload), docker tag (rename/version), docker images (list), docker inspect (details), docker rmi (delete), docker save/load (backup), docker history (layer view), docker commit (container→image).

Each command: purpose, syntax, key flags, examples, real-world usage, interview questions.

Full treatment: 200 MCQs, 200 interview Q&As, 4 labs (beginner to expert), 6 projects, troubleshooting, security, performance.

Commit: `git add phase-02-cli-commands/02-image-commands.md && git commit -m "phase-02: image commands (build, pull, push, tag, etc.)"`
