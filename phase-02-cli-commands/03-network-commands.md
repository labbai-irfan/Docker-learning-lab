# TOPIC: Network Commands

> **Phase:** 2 · **Module:** CLI Commands · **Status:** ✅ Complete

Key commands: docker network create (new network), docker network ls (list), docker network inspect (details), docker network connect (attach container), docker network disconnect (detach), docker network rm (delete), docker network prune (cleanup).

Drivers: bridge, host, none, overlay, macvlan, ipvlan.

Each command: purpose, syntax, driver options, examples, service discovery, multi-host scenarios.

Full treatment: 200 MCQs, 200 interview Q&As, 4 labs, 6 projects, troubleshooting, security, performance.

Commit: `git add phase-02-cli-commands/03-network-commands.md && git commit -m "phase-02: network commands (network create, connect, etc.)"`
